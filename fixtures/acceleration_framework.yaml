plugins:

  # PEFT-related acceleration
  peft:

    # quantization
    quantization: 

      # Q-LORA using autogptq
      auto_gptq:
        kernel: triton_v2
        from_quantized: True

      # Q-LORA using BNB
      # bitsandbytes:
      #   quant_type: nf4 # fp4 